<%= form_for([@video,@comment]) do |f| %>
  

  <div class="field">
    <%=f.hidden_field :time_stamp%>
  </div>
  <div class="field">
    <%= f.text_field :comment_text %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
